export = fixUtf8;
/**
 * A function that tries its best to recover lost Latin Unicode characters
 * @param {string} str -- text to fix
 * @returns {string}
 */
declare function fixUtf8(str: string): string;
